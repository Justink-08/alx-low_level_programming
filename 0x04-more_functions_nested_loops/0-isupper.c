#include "main.h"

/**
 * _isupper(int c) - to check uppercase
 *
 * return : return (1)
 */
int _isupper(int c)
{
	int c, k;

	k = 1;
	
	if (k > 0)
	{
	while (c = 'A'; c <= 'Z'; c++)
	
		_putchar(c);
	}

	_putchar(1);

	return (0);
}
